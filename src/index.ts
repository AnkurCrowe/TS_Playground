function addNumbers(a: number, b: number): number {
    return a + b;
}

function multiplyNumbers(a: number, b: number): number {
    return a * b;
}

function multiplyNumbers1(a: number, b: number) {
    
}

const sumResult = addNumbers1(5, 10);
console.log(`The sum of 5 and 10 is: ${sumResult}`);

const productResult = multiplyNumbers(5, 10);
console.log(`The product of 5 and 10 is: ${productResult}`);
